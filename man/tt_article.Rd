% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{tt_article}
\alias{tt_article}
\title{Format Articles as a TinyTable}
\usage{
tt_article(article, cols = c("title", "summary"), width = c(1, 3))
}
\arguments{
\item{article}{A data frame containing article information (e.g., "title", "summary", "url").}

\item{cols}{A character vector of column names to include in the table. Default is \code{c("title", "summary")}.}

\item{width}{A numeric vector specifying column widths. Default is \code{c(1, 3)}.}
}
\value{
A formatted markdown table as a character string, ready to be displayed in markdown-supported environments.
}
\description{
This function formats a data frame of articles into a markdown-styled table using the \code{tinytable} package.
It allows you to select specific columns from the article data and adjust the column widths for a clean, formatted output.
}
\details{
The function first ensures that the specified columns exist in the data frame and that the length of \code{cols} matches the length of \code{width}.
It then formats the "title" column as a markdown link, using the article's URL (if provided), and selects the requested columns to be displayed in the table.
The resulting table is formatted as markdown for easy integration into markdown environments.
}
\examples{
\dontrun{
papers <- get_articles(journal = "Nature Biotechnology")
papers_with_summary <- add_summary(papers)
tt_article(papers_with_summary)
}

}
\keyword{formatting}
\keyword{markdown}
\keyword{table}
