% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{add_prompt}
\alias{add_prompt}
\title{Add Summarization Prompts to Articles}
\usage{
add_prompt(article, ...)
}
\arguments{
\item{article}{A data frame or tibble containing at least the columns \code{"title"} and \code{"abstract"}.}

\item{...}{Additional arguments passed to \code{build_prompt()}, such as \code{nsentences}.}
}
\value{
A modified data frame of class \code{article_prompt}, including an additional column \code{"prompt"} containing structured summarization prompts.
}
\description{
Adds a prompt column to a data frame of scientific articles, suitable for use with a language model summarization tool.
Prompts are generated using the \code{build_prompt()} function, based on article titles and abstracts.
}
\details{
The function checks for the presence of required columns before proceeding. It applies \code{build_prompt()} row-wise to generate summarization prompts.

This function is typically used after retrieving articles via \code{get_articles()} or \code{get_article()}, to prepare data for summarization by a language model (e.g., using \code{ollama::generate()}).
}
\examples{
\dontrun{
papers <- get_articles("Nature Medicine")
papers_with_prompts <- add_prompt(papers, nsentences = 3)
cat(papers_with_prompts$prompt[1])
}

}
\keyword{prompt-engineering}
